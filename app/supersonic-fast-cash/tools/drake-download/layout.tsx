import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Drake Download | Supersonic Fast Cash',
  description: 'Download Drake tax software for professional tax preparation.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
