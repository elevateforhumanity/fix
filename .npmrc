enable-pre-post-scripts=true
# Required by @netlify/plugin-nextjs for pnpm + standalone output to work correctly.
# Without this, the plugin cannot resolve node_modules in the standalone directory.
# See: https://docs.netlify.com/integrations/frameworks/next-js/overview/#pnpm-support
public-hoist-pattern[]=*
